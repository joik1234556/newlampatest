{
  "name": "easy-mods-backend",
  "version": "2.0.0",
  "description": "Easy-mods backend: search + stream via TorrServer",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "check": "node --check server.js && node --check routes/api.js && node --check routes/balancers.js && node --check config.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  }
}
